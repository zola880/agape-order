# Food Order App

Simple React + Vite app for ordering food (demo).

Run locally:

```bash
cd Agape/food-order-app
npm install
npm run dev
```

Open the URL printed by Vite (usually http://localhost:5173).
